# Fixtures

`issues.json` is generated by `pnpm build-fixtures`. It expects 25 hand-picked
issues from `shadcn-ui/ui` covering this distribution per ROADMAP.md:

| count | category   | rationale |
|-------|------------|-----------|
| 8     | bug        | issues with linked fix PRs that changed concrete files; gives non-empty `expectedFiles` |
| 5     | feature    | feature requests closed via PR; `expectedFiles` show where the feature shipped |
| 4     | docs / question | low-stakes triage where `expectedFiles` is often empty and `expectedCategory` is the metric |
| 4     | duplicate  | issues closed with `closes #N` style links; `expectedSimilarIssues` non-empty |
| 2     | wontfix    | declined feature requests / out-of-scope reports |
| 2     | invalid    | misfilings, spam, environment-only problems |

The issue refs used to seed the file are the `ISSUE_REFS` constant in
`scripts/build-fixtures.ts`. The current 30 entries are placeholder numbers
spread across the project's history; before relying on the eval signal you
should:

1. Skim each issue's GitHub page.
2. Replace any that don't match the desired category buckets.
3. Re-run `pnpm build-fixtures` after editing the array.
4. Keep exactly 25 entries with the diversity spelled out above.
5. Append a one-line rationale per issue at the bottom of this file once you
   have settled on the final 25 — that is the deliverable per ROADMAP.md.

## Final 25 (fill in)

| ref | category | one-line rationale |
|-----|----------|--------------------|
|     |          |                    |
